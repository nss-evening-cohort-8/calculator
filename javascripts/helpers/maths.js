const multiply = (n1, n2) => {
  return n1 * n2;
};

const add = (n1, n2) => {
  return n1*1 + n2*1;
};

const subtract = (n1, n2) => {
  return n1*1 - n2*1;
};

const divide = (n1, n2) => {
  return n1 / n2;
};
export {add, divide, multiply, subtract};